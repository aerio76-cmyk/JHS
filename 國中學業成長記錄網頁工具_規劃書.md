# 國中學業成長記錄網頁工具 — 系統規劃書

> 版本 v1.0｜依據需求初稿重新設計優化

---

## 一、專案概述與目標

| 項目 | 內容 |
|---|---|
| 使用對象 | 家中兩位國中學生（前台觀看），家長（後台維護） |
| 核心價值 | 把 3 年 18 次段考成績轉化為「看得懂的成長故事」，而不是冷冰冰的數字表 |
| 視覺風格 | 可愛手繪風（Doodle Style），降低成績帶來的心理壓力，強調鼓勵而非評比 |
| 資料週期 | 3 個年級 × 2 學期 × 3 次段考 = 共 18 次記錄點 |

**設計原則（比原稿多補一條）**：整個系統應該讓孩子「想打開來看」，而不是害怕打開。因此後面的分析邏輯與手足比較功能，會刻意做成「預設關閉／以鼓勵為主」，避免變相排名。

---

## 二、技術架構建議

原稿方案是「GitHub Pages 靜態網頁 + Supabase」，可行，但你之前已經用 **Next.js + Supabase + Google OAuth + Vercel** 完成過 Molos-Web，這套組合你已經踩過坑、也熟悉部署流程。這裡列出兩個選項給你評估：

### 方案 A：沿用 Molos-Web 熟悉的組合（建議）
- **前端**：Next.js（App Router）+ Tailwind CSS
- **資料庫/驗證**：Supabase（PostgreSQL + Auth）
- **部署**：Vercel
- **優點**：Google OAuth callback、API Route、環境變數管理你都已經處理過一次，開發速度會比重新學一套純靜態架構快；未來要加「PDF 匯出成績單」「排程提醒」等功能也有 Serverless Function 可用。
- **缺點**：比純靜態多一點建置設定。

### 方案 B：原稿的純靜態 + GitHub Pages
- **前端**：HTML/CSS/JS（無框架）或純 React SPA
- **資料庫/驗證**：Supabase（前端直接呼叫 Supabase JS SDK，Auth 也是純前端流程）
- **部署**：GitHub Pages
- **優點**：完全免費、沒有伺服器概念，最單純。
- **缺點**：所有邏輯都在瀏覽器端，日後想加「產生 PDF 報告」「排程寄信」等需要後端運算的功能會比較麻煩，需要另外找 Serverless 服務。

> **建議**：若這個專案預期會長期擴充（例如之後想做通知、匯出報告），選 **方案 A**；若你就是想要一個「做完就穩定、幾乎不再動」的小工具，**方案 B** 也完全夠用。兩者的資料庫設計（第三節）完全通用，可以先定資料庫，前端框架晚點再決定。

### 圖表套件
- 建議用 **Chart.js**（生態成熟、客製化文件多），若走方案 A（React 環境）也可換成 **Recharts**。手繪筆觸效果可另外疊加 **RoughJS** 或用 CSS 濾鏡做出「鉛筆邊框」的視覺，避免直接用 RoughJS 畫真正的資料線（RoughJS 的隨機抖動可能讓數據點位置失真，建議手繪風只用在裝飾元素，不用在資料曲線本身）。

---

## 三、資料庫設計（Supabase Schema）

在你原本兩張表的基礎上做以下調整：

### 1. `students`（學生資料表）— 沿用附圖設計
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid (PK) | 學生唯一識別碼 |
| name | text | 學生姓名/暱稱 |
| avatar_url | text | 手繪頭像網址（建議存在 Supabase Storage） |
| enrolled_year | int2 | 入學年度（用來自動換算目前是幾年級，避免每年手動改） |

### 2. `exam_scores`（成績紀錄表）— 沿用附圖 + 補強
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | bigint (PK) | 成績紀錄 ID |
| student_id | uuid (FK) | 關聯至 students.id |
| grade | int2 | 年級 (1,2,3) |
| semester | int2 | 學期 (1:上學期, 2:下學期) |
| exam_type | int2 | 段考序號 (1,2,3) |
| subject | text | 科目 |
| score | numeric | 得分 |
| **full_score** | numeric | **新增：該次考試滿分**（不是每次都剛好 100 分，之後算百分比才準確） |
| **exam_date** | date | **新增：實際考試日期**（選填，圖表 X 軸可用真實時間而非只用序號） |
| **updated_at** | timestamp | **新增：最後修改時間**（家長訂正分數時可追蹤） |
| created_at | timestamp | 建立時間 |

### 3. `subjects`（新增：科目對照表）
避免每次手動輸入科目名稱時打錯字（"國文" vs "國語"），用下拉選單統一：
| 欄位 | 型別 | 說明 |
|---|---|---|
| code | text (PK) | 科目代碼（如 chi, eng, math） |
| display_name | text | 顯示名稱（國文、英文、數學…） |
| sort_order | int2 | 顯示排序 |

### 4. `motivational_quotes`（新增：勵志小語資料庫）
把小語從寫死在程式碼裡改成存資料庫，之後想新增、修改文字完全不用改程式：
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | bigint (PK) | 小語 ID |
| category | text | 對應情境標籤（progress / stable / decline / final_exam…） |
| content | text | 小語內容 |
| source | text | 出處（選填，尊重原創） |
| illustration_key | text | 對應的手繪插圖檔名，讓小語跟圖一起換 |
| is_active | bool | 是否啟用（不想用某句可直接關閉，不用刪除） |

### 5. `exam_summaries`（新增：總評歷史存檔，選用）
每次生成的總評文字存下來，之後前台可以做「回顧時光機」功能，回看每次考完當下系統怎麼鼓勵他：
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | bigint (PK) | ID |
| student_id | uuid (FK) | 關聯學生 |
| grade / semester / exam_type | int2 | 對應哪一次段考 |
| summary_text | text | 當時生成的總評文字 |
| tag | text | 判定的標籤（進步爆發 / 穩定 / 遭遇瓶頸…） |
| created_at | timestamp | 生成時間 |

### RLS（Row Level Security）安全性設計
- **讀取（SELECT）**：`students`、`exam_scores`、`motivational_quotes` 開放公開讀取（前台看板免登入），但只回傳非敏感欄位。
- **寫入（INSERT/UPDATE/DELETE）**：僅限已透過 Supabase Auth（Google OAuth）登入且帳號在白名單內的家長帳號。**不建議**開放「直接進到資料庫填寫」作為正式管道——那個方式沒有輸入驗證（例如分數打成負數、科目名稱打錯），長期維護容易出錯，建議只留一個真正的輸入入口，也就是需求 3 裡的「後台頁面登入填寫」。Supabase Dashboard 可以保留當作緊急修資料用，但不當日常入口。

---

## 四、功能模組與頁面規劃

### 1. 學生前台（免登入看板）
- **學生切換**：頂部 Tab 或頭像點選，切換兩位學生的專屬看板
- **學習曲線主圖**
  - 總分/平均趨勢線：18 次段考完整走勢
  - 單科趨勢線：可複選科目疊加比較（例如同時看數學、英文 3 年變化）
- **多維度比較分析**
  - 學期內比較：同一學期 3 次段考的走勢 mini 圖
  - 手足比較（**預設關閉**，家長可在設定中開啟）：若開啟，呈現方式建議用「兩人各自的進步幅度」而非「分數高低排名」，降低比較壓力
- **學習總評卡片**：依第五節邏輯自動生成
- **勵志小語 + 吉祥物**：對話框呈現，插圖隨情境切換

### 2. 家長後台（Supabase Auth 登入）
- **登入**：Google OAuth（沿用你在 Molos-Web 已經熟悉的流程）
- **成績輸入表單**：選學生 → 選年級/學期/段考次數 → 一次輸入各科分數與滿分 → 送出後前端做基本驗證（分數不可為負、不可超過滿分）再寫入 Supabase
- **歷史紀錄管理**：表格檢視、可編輯/刪除，修改時自動更新 `updated_at`
- **小語內容管理**：CRUD `motivational_quotes`，家長可以自己新增專屬孩子的鼓勵語
- **學生資料管理**：頭像上傳（Supabase Storage）、基本資料維護

---

## 五、總評分析邏輯設計

原稿用固定門檻（例如「進步超過 X 分」）容易對不同科目、不同孩子不公平（有人本來就常態 90 分以上，進步空間小）。建議改用**相對於自己歷史表現**的判斷方式：

1. **計算基準**：取該學生、該科目（或總分）過去所有已記錄考試的平均值與標準差
2. **判定邏輯**（門檻建議做成後台可調整的設定值，方便你依實際狀況微調）：
   - 本次成績 ≥ 歷史平均 + 1 個標準差 → **進步爆發型**
   - 本次成績落在歷史平均 ± 0.5 個標準差內 → **穩定發揮型**
   - 本次成績 ≤ 歷史平均 − 1 個標準差 → **暫時遭遇瓶頸**
   - 單科表現特別突出（相對自己該科過去最高分再突破）→ **單科黑馬型**
   - 資料筆數不足（例如剛開始記錄，還沒有足夠歷史數據）→ 改用「這是我們記錄的第一步，之後可以一起看你的成長軌跡！」這類開場型文案，避免用不成熟的統計亂貼標籤

3. **文案語氣**：延續你原稿的方向，全程正向、聚焦「努力」與「過程」而非單純結果，並避免使用「退步」「差」這類字眼，改用「這次遇到比較有挑戰的題目」等中性描述。

---

## 六、勵志小語匹配規則

- 依第五節判定出的 `tag`，從 `motivational_quotes` 資料表中篩出對應 `category` 的所有小語
- 隨機挑選一則，並記錄「最近使用過的小語 ID」（可存 local storage 或簡易 log 表），下次同分類避免重複挑到同一句，直到清單用完才重複
- 學期末最後一次段考（exam_type = 3 且為該學期最後一次）可額外疊加「迎戰期末/展望下學期」的專屬分類

初始語句庫建議至少每個分類準備 **8–10 句**，避免孩子很快就看膩重複的話（原稿範例每類只有 1 句，數量偏少，建議之後擴充）。

---

## 七、UI/UX 設計方向

- **配色**：建議用馬卡龍色系或粉彩色（薄荷綠、鵝黃、粉橘），搭配白底，符合「可愛不幼稚」的國中生年齡層
- **字體**：中文建議使用開源的 **jf open 粉圓（justfont open 粉圓）**，這是台灣很常見、免費可商用的圓體手寫感字型，相容性比許多「手寫體」Google Fonts 對繁體中文字的支援更完整；英數字可搭配 Google Fonts 的 "Patrick Hand" 或 "Kalam" 增加手寫感
- **裝飾元素**：
  - 進度/成就徽章：完成一次段考解鎖一個手繪徽章（共 18 個），可用簡單的 SVG 插畫
  - 吉祥物：頁面角落放一隻手繪小動物，用對話框顯示小語，讓整體更有「陪伴感」而非「監督感」
- **響應式設計**：國中生大機率用手機查看，前台務必做手機版優化，圖表在小螢幕上要能左右滑動查看 18 個資料點

---

## 八、開發階段規劃

| 階段 | 內容 | 產出 |
|---|---|---|
| 1. 資料庫建置 | 建立 Supabase 專案、5 張表、RLS 權限、測試 Google OAuth 登入 | 可用的資料庫 + 驗證流程 |
| 2. 後台輸入功能 | 開發成績輸入表單、歷史紀錄管理、資料驗證邏輯 | 家長可完整維護資料 |
| 3. 前台圖表 | 串接 Chart.js，完成主曲線圖、單科比較、學期內比較 | 圖表可視化完成 |
| 4. 分析與小語系統 | 撰寫統計判定邏輯、小語匹配、總評卡片 | 自動生成總評與鼓勵語 |
| 5. 手繪視覺與部署 | 套用可愛風格 CSS、字體、插畫、響應式調整，正式部署 | 上線可用版本 |
| 6.（選用）迭代優化 | 依實際使用回饋調整判定門檻、擴充小語庫、加成就徽章動畫 | 長期優化 |

---

## 九、待你確認的決策點

在正式開發前，建議先拍板以下幾點，會影響第一階段的資料庫細節：

1. **前端架構**：選方案 A（Next.js + Vercel，沿用 Molos-Web 經驗）還是方案 B（純靜態 + GitHub Pages）？
2. **前台是否需要密碼保護**：目前設計前台免登入即可看到成績，若這個網址可能被其他人看到，是否要加一層簡易密碼（不需要完整帳號系統，一組共用密碼即可）？
3. **各科滿分是否固定 100 分**：如果都是固定的，`full_score` 欄位可以省略，簡化輸入流程。
4. **手足比較功能**：預設關閉、家長手動開啟的做法是否符合你的想法？或是完全不做這個功能？
5. **是否需要匯出功能**：例如學期末產出一張 PDF 成長報告單（如果需要，方案 A 會比較容易做這個功能）。

這五點確認後，我可以直接幫你把資料庫建表 SQL、Next.js（或純靜態）專案骨架寫出來。
